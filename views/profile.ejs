<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-between items-center mb-2">
        <h4 class="text-lg mb-2">Your Profile</h4>
        <a href="/logout" class="bg-red-500 rounded-lg px-4 py-2">Logout</a>
      </div>


      <div class="flex gap-4 content-center justify-start mb-5">
        <div>
          <img
            src="images/uploads/<%= user.profileImg %> "
            alt="profile"
            class="rounded-full w-12 h-12 object-cover"
          />

        </div>

        <h3 class="text-3xl font-bold ">Hello <%= user.name %> 👋🏻</h3>
        </div>
        
        <a href="/profile/upload" class="text-blue-500">Upload Profile Picture From Here 🔼 </a>
      <h5 class="text-md font-sm mb-3 mt-2">
        Greate! You Can create new posts now.
      </h5>

      <form method="post" action="/create-post">
        <textarea
          placeholder = "Hey... What's on your mind?"
          class="block w-1/3 resize-none bg-transparent border-2 outline-none border-zinc-800 rounded-lg p-2 text-white"
          name="content"
          id="posts"
          value="Hey... What's on your mind?"
        /></textarea>

        <button
          class="block bg-blue-800 text-white px-4 py-2 rounded-md mt-3 w-40"
          type="submit"
        >Create New Post</button>
      </form>

      <h4 class="text-lg mb-2 mt-10">Your Posts</h4>
      <div class=" flex gap-5 flex-wrap">
          
        <% user.posts.reverse().forEach(post => { %>
          <div class="border-2 border-zinc-800 rounded-lg w-1/3 p-3 bg-zinc-800 mb-4">
            <h4 class="text-md text-blue-500 mb-2">@<%=  user.username %> </h4>
            <p class="text-sm tracking-tight mb-2 "> <%= post.content %> </p>
            <div class="flex  gap-4">
    
              <a class="text-sm text-blue-500 " href="/edit/<%= post._id %>">Edit</a>
              <a class="text-sm text-red-500 " href="/delete/<%= post._id %>">Delete</a>
            </div>
        </div>
        
        <% }) %>

      </div>
  </body>
  <script>
  </script>
</html>
