<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-between items-center mb-2">
        <h4 class="text-lg mb-2">Edit Post</h4>
        <a href="/logout" class="bg-red-500 rounded-lg px-4 py-2">Logout</a>
      </div>

      <form method="post" action="/edit/<%= post._id %>">
        <textarea
          placeholder = "Hey... What's on your mind?"
          class="block w-1/3 resize-none bg-transparent border-2 outline-none border-zinc-800 rounded-lg p-2 text-white"
          name="content"
          id="posts"
        
        /><%= post.content %></textarea>

        <button
          class="block bg-yellow-600 text-white px-4 py-2 rounded-md mt-3 w-40"
          type="submit"
        >Update Post</button>
      </form>

      </div>
  </body>
  <script>
  </script>
</html>
